import { writable } from 'svelte/store';

export const about = writable({ glow: false })
export const tech = writable({ glow: false })
export const experience = writable({ glow: false })
export const projects = writable({ glow: false })
export const contact = writable({ glow: false })